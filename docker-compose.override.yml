services:
  web:
    working_dir: /app
    command: bash -c "./wait_for_db.sh && python manage.py migrate --noinput && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./src:/app
    ports:
      - "8000:8000"
